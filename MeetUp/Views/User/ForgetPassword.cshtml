@model MeetUp.Models.Users;
@{
    Layout = null;
}

<!DOCTYPE html>
<html5>

<head>
    <title>Forget Password</title>
    <link href="https://fonts.googleapis.com/css?family=Fruktur&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Itim&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-sweetalert/1.0.1/sweetalert.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-sweetalert/1.0.1/sweetalert.css" rel="stylesheet" />
    <link href="~/css/forgot_pass1.css" rel="stylesheet"/>
</head>

<body>
    <div class="logo">
        <a href="@Url.Action("Index", "Home")"><img src="~/img/Logo.png"></a>
    </div>
    <div class="container">
        <div class="corner">
            <img src="~/img/corner.png">
        </div>
        <div class="form_box">
            <form class="input_form" id="MyForm">
                <img class="lock_icon" src="~/img/lock_icon.png" alt="Lock Icon"><br>
                <span class="form_heading">Forgot password ?</span>
                <p class="form_desc">No worries, we will send you reset instructions.</p>
                <div class="email_box">
                        @Html.TextBoxFor(m => m.email, new { @class = "input_email", @type="email", @placeholder = "Please Enter Email", @required = "required"});
                <div class="error_text">
                    
                </div>
                </div>
                <div class="btn_box">
                        <button class="reset_btn" id="btn_reset">Reset Password</button>
                </div>
                <div class="footer_link">
                    <a class="back_login" href="@Url.Action("Login", "user")">
                        <img class="back_arrow" src="~/img/back_orange.png" alt="Back">
                        <span>Back to Log in</span>
                    </a>
                </div>
            </form>
        </div>
    </div>
</body>